import { CreateProjectSchema, UpdateProjectSchema } from "@repo/schemas"

export { CreateProjectSchema, UpdateProjectSchema }
export type { CreateProjectInput, UpdateProjectInput } from "@repo/schemas"
